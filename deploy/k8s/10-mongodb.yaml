# MongoDB Atlas is used as the database — no local MongoDB needed.
# MONGO_URL is stored in backend-secrets and points to Atlas.
# This file is intentionally empty so the deploy pipeline still works.
